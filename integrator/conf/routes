# Routes
# ~~~~

GET     /view/:ep                   controllers.IncomingRouter.view(ep: String)
GET     /read/:ep/:stream           controllers.IncomingRouter.read(ep: String, stream: String)
GET     /write/:ep                  controllers.IncomingRouter.write(ep: String)
GET     /menu                       controllers.MenuController.index()
